# Infrastructure Layer - Notifications

This directory contains infrastructure-level notification services.

## Purpose

The infrastructure layer provides cross-cutting concerns that support the domain and data layers. Notifications are a critical infrastructure component that should be available throughout the application.

## Migration Notes

This directory will contain notification services migrated from `core/notifications/` as part of the architecture improvement plan.

