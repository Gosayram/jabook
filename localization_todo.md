# Localization Todo List

## Статус локализации

Этот файл содержит полный список всех найденных хардкодных строк в проекте, которые необходимо локализовать.

**Последнее обновление:** 2025-11-23 (после изучения всей кодовой базы)
**Всего секций:** 40
**Статус:** Актуально - все файлы проверены

## Hardcoded Strings Found in Codebase

### 1. Debug Screen (lib/features/debug/presentation/screens/debug_screen.dart)

- ✅ "Debug Tools" - appBar title (уже есть debugToolsTitle)
- ✅ "Logs" - tab text (уже есть logsTab)
- ✅ "Mirrors" - tab text (уже есть mirrorsTab)
- ✅ "Downloads" - tab text (уже есть downloadsTab)
- ✅ "Cache" - tab text (уже есть cacheTab)
- ✅ "Test all mirrors" - button text (уже есть testAllMirrorsButton)
- ✅ "Status: $statusText" - mirror status text (уже есть statusLabelText)
- ✅ "Last OK: $lastOk" - mirror status text (уже есть lastOkLabelText)
- ✅ "RTT: $rtt ms" - mirror status text (уже есть rttLabelText, millisecondsText)
- ✅ "Download ${download['id']}" - download title (уже есть downloadLabelText)
- ✅ "Status: $status" - download status (уже есть statusLabel)
- ✅ "Progress: ${progress.toStringAsFixed(1)}%" - download progress (уже есть downloadProgressLabelText)
- ✅ "Delete download" - delete button (уже есть deleteDownloadButton)
- ✅ "Cache Statistics" - section title (уже есть cacheStatisticsTitle)
- ✅ "Total entries: " - cache stat label (уже есть totalEntriesText)
- ✅ "Search cache: " - cache stat label (уже есть searchCacheText)
- ✅ "Topic cache: " - cache stat label (уже есть topicCacheText)
- ✅ "Memory usage: " - cache stat label (уже есть memoryUsageText)
- ✅ "Clear all cache" - button text (уже есть clearAllCacheButton)
- ✅ "Cache is empty" - empty cache message (уже есть cacheIsEmptyMessage)
- ✅ "Refresh debug data" - refresh button (уже есть refreshDebugDataButton)
- ✅ "Export logs" - export button (уже есть exportLogsButton)
- ✅ "Logs exported successfully" - success message (уже есть logsExportedSuccessfullyMessage)
- ✅ "Failed to export logs: $e" - error message (уже есть failedToExportLogsMessage)
- ✅ "Cache cleared successfully" - success message (уже есть cacheClearedSuccessfullyMessage)
- ✅ "Failed to clear cache: $e" - error message (добавлено failedToClearCacheMessage)
- ✅ "Mirror health check completed" - success message (уже есть mirrorHealthCheckCompletedMessage)
- ✅ "Failed to test mirrors: $e" - error message (добавлено failedToTestMirrorsMessage)
- ✅ "Degraded" - mirror status (добавлено mirrorStatusDegraded)
- ✅ "Unhealthy" - mirror status (добавлено mirrorStatusUnhealthy)

### 2. WebView Screen (lib/features/webview/secure_rutracker_webview.dart, rutracker_login_screen.dart)

- ✅ "RuTracker" - appBar title (уже есть webViewTitle)
- ✅ "Этот сайт использует проверки безопасности Cloudflare..." - cloudflare message (уже есть cloudflareMessage)
- ✅ "Повторить попытку" - retry button (уже есть retryButtonText)
- ✅ "Перейти на главную" - home button (уже есть goHomeButtonText)
- ✅ "Сайт проверяет ваш браузер..." - browser check message (уже есть browserCheckMessage)
- ✅ "Скачать торрент" - dialog title (уже есть downloadTorrentTitle)
- ✅ "Выберите действие:" - dialog content (уже есть selectActionText)
- ✅ "Открыть" - open button (уже есть openButtonText)
- ✅ "Скачать" - download button (уже есть downloadButtonText)
- ✅ "Для скачивания файла, пожалуйста, откройте ссылку в браузере" - download message (уже есть downloadInBrowserMessage)
- ✅ "URL недоступен" - error message (добавлено urlUnavailable)
- ✅ "Неверный формат URL: $urlString" - error message (добавлено invalidUrlFormat)
- ✅ "Ошибка: $e" - error message (добавлено genericError)
- ✅ "Повторная попытка подключения ($_retryCount/$_maxRetries)..." - retry message (уже есть retryConnectionMessage)
- ✅ "Ошибка загрузки: $desc" - error message (уже есть loadError)
- ✅ "Произошла ошибка при загрузке страницы" - error message (уже есть pageLoadError)
- ✅ "Security verification in progress - please wait..." - security message (добавлено securityVerificationInProgress)
- ✅ "Open in Browser" - button text (добавлено openInBrowserButton)
- ✅ "Login successful!" - success message (добавлено loginSuccessfulMessage)
- ✅ "Для загрузки файл будет открыт в браузере" - download message (уже есть fileWillOpenInBrowser)

### 3. Library Screen (lib/features/library/presentation/screens/library_screen.dart)

- ✅ "Import Audiobooks" - dialog title (уже есть importAudiobooksTitle)
- ✅ "Select audiobook files from your device to add to your library" - dialog content (уже есть selectFilesMessage)
- ✅ "Cancel" - cancel button (уже есть cancel)
- ✅ "Import" - import button (уже есть importButtonText)
- ✅ "Imported $importedCount audiobook(s)" - success message (уже есть importedSuccess)
- ✅ "No files selected" - no files message (уже есть noFilesSelectedMessage)
- ✅ "Failed to import: $e" - error message (уже есть importFailedMessage)
- ✅ "Scan Folder" - dialog title (уже есть scanFolderTitle)
- ✅ "Scan a folder on your device for audiobook files" - dialog content (уже есть scanFolderMessage)
- ✅ "Scan" - scan button (уже есть scanButtonText)
- ✅ "Found and imported $importedCount audiobook(s)" - success message (уже есть scanSuccessMessage)
- ✅ "No audiobook files found in selected folder" - no files message (уже есть noAudiobooksFoundMessage)
- ✅ "No folder selected" - no folder message (уже есть noFolderSelectedMessage)
- ✅ "Failed to scan folder: $e" - error message (уже есть scanFailedMessage)
- ✅ "Избранное" - favorites tooltip (добавлено favoritesTooltip)
- ✅ "Filter library" - filter tooltip (добавлено filterLibraryTooltip)
- ✅ "Add audiobook" - FAB tooltip (добавлено addAudiobookTooltip)
- ✅ "Your library is empty" - empty library message (добавлено libraryEmptyMessage)
- ✅ "Add audiobooks to your library to start listening" - empty library hint (добавлено addAudiobooksHint)
- ✅ "Search for audiobooks" - button label (добавлено searchForAudiobooksButton)
- ✅ "Import from Files" - button label (добавлено importFromFilesButton)
- ✅ "Scan Folder" - button label (уже есть scanFolderTitle, используется)

### 4. Auth Screen (lib/features/auth/presentation/screens/auth_screen.dart)

- ✅ "RuTracker Connection" - appBar title (уже есть authScreenTitle)
- ✅ "Username" - label (уже есть usernameLabelText)
- ✅ "Password" - label (уже есть passwordLabelText)
- ✅ "Remember me" - checkbox text (уже есть rememberMeLabelText)
- ✅ "Login" - login button (уже есть loginButtonText)
- ✅ "Test Connection" - test button (уже есть testConnectionButtonText)
- ✅ "Logout" - logout button (уже есть logoutButtonText)
- ✅ "Login to RuTracker to access audiobook search and downloads. Your credentials are stored securely." - help text (уже есть authHelpText)
- ✅ "Please enter username and password" - status message (добавлено pleaseEnterCredentials)
- ✅ "Logging in..." - status message (уже есть loggingInText)
- ✅ "Login successful!" - success message (уже есть loginSuccessMessage)
- ✅ "Login failed. Please check credentials" - failure message (уже есть loginFailedMessage)
- ✅ "Login error: ${e.toString()}" - error message (уже есть loginErrorMessage)
- ✅ "Testing connection..." - status message (добавлено testingConnectionText)
- ✅ "Connection successful! Using: $activeEndpoint" - success message (уже есть connectionSuccessMessage)
- ✅ "Connection test failed: ${e.toString()}" - error message (уже есть connectionFailedMessage)
- ✅ "Logging out..." - status message (уже есть loggingOutText)
- ✅ "Logged out successfully" - success message (уже есть logoutSuccessMessage)
- ✅ "Logout error: ${e.toString()}" - error message (уже есть logoutErrorMessage)

### 5. Settings Screen (lib/features/settings/presentation/screens/settings_screen.dart)

- ✅ "Manage Mirrors" - subtitle (уже есть mirrorsScreenTitle)
- ✅ "Configure and test RuTracker mirrors" - subtitle (добавлено configureMirrorsSubtitle, используется)
- ✅ "Playback Speed" - title (уже есть playbackSpeedTitle)
- ✅ "1.0x" - subtitle (уже есть playbackSpeedDefault)
- ✅ "Skip Duration" - title (уже есть skipDurationTitle)
- ✅ "15 seconds" - subtitle (уже есть skipDurationDefault)
- ✅ "Download Location" - title (уже есть downloadLocationTitle)
- ⚠️ "/storage/emulated/0/Download" - subtitle (пример пути, не нужно локализовать)
- ✅ "Dark Mode" - title (уже есть darkModeTitle)
- ✅ "High Contrast" - title (уже есть highContrastTitle)
- ✅ "Wi-Fi Only Downloads" - title (уже есть wifiOnlyDownloadsTitle)
- ✅ "Управление сессией RuTracker (cookie)" - section description (уже есть rutrackerSessionDescription)
- ✅ "Войти в RuTracker через WebView" - button text (добавлено webViewLoginButton)
- ✅ "Пройти Cloudflare/капчу и сохранить cookie для клиента" - button subtitle (добавлено webViewLoginSubtitle)
- ✅ "Cookie сохранены для HTTP-клиента" - success message (добавлено cookieSavedMessage)
- ✅ "Очистить сессию RuTracker (cookie)" - button text (уже есть clearSessionButton)
- ✅ "Удалить сохранённые cookie и выйти из аккаунта" - button subtitle (уже есть clearSessionSubtitle)
- ✅ "Сессия RuTracker очищена" - success message (уже есть sessionClearedMessage)
- ✅ "Метаданные аудиокниг" - section title (уже есть metadataSectionTitle)
- ✅ "Управление локальной базой метаданных аудиокниг" - section description (уже есть metadataSectionDescription)
- ✅ "Всего записей" - stat label (уже есть totalRecordsLabel)
- ✅ "Последнее обновление" - stat label (уже есть lastUpdateLabel)
- ✅ "Обновление..." - button text (уже есть updatingText)
- ✅ "Обновить метаданные" - button text (уже есть updateMetadataButton)
- ✅ "Начато обновление метаданных..." - status message (уже есть metadataUpdateStartedMessage)
- ✅ "Обновление завершено: собрано $total записей" - success message (уже есть metadataUpdateCompletedMessage)
- ✅ "Ошибка обновления: $e" - error message (уже есть metadataUpdateError)
- ✅ "Никогда" - date text (уже есть neverDate)
- ✅ "Только что" - date text (уже есть justNow)
- ✅ "Неизвестно" - date text (уже есть unknownDate)
- ✅ "X дн. назад" - date format (уже есть daysAgo)
- ✅ "X ч. назад" - date format (уже есть hoursAgo)
- ✅ "X мин. назад" - date format (уже есть minutesAgo)
- ✅ "App Permissions" - section title (уже есть appPermissionsTitle)
- ✅ "Storage" - permission name (уже есть storagePermissionName)
- ✅ "Save audiobook files and cache data" - permission description (уже есть storagePermissionDescription)
- ✅ "Notifications" - permission name (уже есть notificationsPermissionName)
- ✅ "Show playback controls and updates" - permission description (уже есть notificationsPermissionDescription)
- ✅ "Grant All Permissions" - button text (уже есть grantAllPermissionsButton)
- ✅ "All permissions granted" - success message (уже есть allPermissionsGranted)
- ✅ "File access available" - status message (уже есть fileAccessAvailable)
- ✅ "File access unavailable" - status message (уже есть fileAccessUnavailable)
- ✅ "Notifications available" - status message (уже есть notificationsAvailable)
- ✅ "Notifications unavailable" - status message (уже есть notificationsUnavailable)
- ✅ "Capabilities: $grantedCount/${results.length}" - status message (уже есть capabilitiesStatus)
- ✅ "Backup & Restore" - section title (уже есть backupRestoreTitle)
- ✅ "Export and import your data (favorites, history, metadata)" - section description (уже есть backupRestoreDescription)
- ✅ "Export Data" - button text (уже есть exportDataButton)
- ✅ "Save all your data to a backup file" - button subtitle (уже есть exportDataSubtitle)
- ✅ "Import Data" - button text (уже есть importDataButton)
- ✅ "Restore data from a backup file" - button subtitle (уже есть importDataSubtitle)
- ✅ "Exporting data..." - status message (уже есть exportingDataMessage)
- ✅ "Data exported successfully" - success message (уже есть dataExportedSuccessfullyMessage)
- ✅ "Failed to export: ${e.toString()}" - error message (уже есть failedToExportMessage)
- ✅ "Import Backup" - dialog title (уже есть importBackupTitle)
- ✅ "Importing data..." - status message (уже есть importingDataMessage)
- ✅ "Failed to import: ${e.toString()}" - error message (уже есть failedToImportMessage)
- ✅ "Clear Expired Cache" - button text (уже есть clearExpiredCacheButton)
- ✅ "Language changed to ${languageName}" - success message (уже есть languageChangedMessage)

### 6. Mirror Settings Screen (lib/features/settings/presentation/screens/mirror_settings_screen.dart)

- ✅ "Failed to load mirrors: $e" - error message (уже есть failedToLoadMirrorsMessage)
- ✅ "Mirror $url tested successfully" - success message (уже есть mirrorTestSuccessMessage)
- ✅ "Failed to test mirror $url: $e" - error message (уже есть mirrorTestFailedMessage)
- ✅ "Mirror ${enabled ? 'enabled' : 'disabled'}" - status message (уже есть mirrorStatusText)
- ✅ "Failed to update mirror: $e" - error message (уже есть failedToUpdateMirrorMessage)
- ✅ "Add Custom Mirror" - dialog title (уже есть addCustomMirrorTitle)
- ✅ "Mirror URL" - label (уже есть mirrorUrlLabelText)
- ✅ "https://rutracker.example.com" - hint text (уже есть mirrorUrlHintText)
- ✅ "Priority (1-10)" - label (уже есть priorityLabelText)
- ✅ "5" - hint text (уже есть priorityHintText)
- ✅ "Add" - add button (уже есть addMirrorButtonText)
- ✅ "Mirror ${urlController.text} added" - success message (уже есть mirrorAddedMessage)
- ✅ "Failed to add mirror: $e" - error message (уже есть failedToAddMirrorMessage)
- ✅ "Mirror Settings" - appBar title (уже есть mirrorSettingsTitle)
- ✅ "Configure RuTracker mirrors for optimal search performance. Enabled mirrors will be used automatically." - description (уже есть mirrorSettingsDescription)
- ✅ "Add Custom Mirror" - button text (уже есть addCustomMirrorButtonText)
- ✅ "Priority: $priority" - priority text (уже есть priorityText)
- ✅ "Response time: $rtt ms" - response time text (уже есть responseTimeText)
- ✅ "Last checked: ${\_formatDate(lastOk)}" - last check text (уже есть lastCheckedText)
- ✅ "Test this mirror" - button text (уже есть testMirrorButtonText)
- ✅ "Active" - active status (уже есть activeStatusText)
- ✅ "Disabled" - disabled status (уже есть disabledStatusText)
- ✅ "Never" - never date (уже есть neverDateText)
- ✅ "Invalid date" - invalid date message (уже есть invalidDateText)
- ✅ "Нет активных зеркал для проверки" - error message (уже есть noActiveMirrorsMessage)
- ✅ "Проверка завершена: $tested/$total зеркал" - success message (уже есть mirrorHealthCheckCompletedMessage)
- ✅ "Активное зеркало установлено: $url" - success message (уже есть activeMirrorSetMessage)
- ✅ "Не удалось установить активное зеркало: $e" - error message (уже есть failedToSetActiveMirrorMessage)
- ✅ "Изменить приоритет" - dialog title (уже есть editPriorityTitle)
- ✅ "URL: $url" - dialog content (добавлено urlLabel)
- ✅ "Приоритет (1-10)" - label (уже есть priorityLabelText)
- ✅ "Меньше число = выше приоритет" - helper text (уже есть priorityHelperText)
- ✅ "Сохранить" - save button (уже есть saveButtonText)
- ✅ "Приоритет обновлен: $priority" - success message (уже есть priorityUpdatedMessage)
- ✅ "Не удалось обновить приоритет: $e" - error message (уже есть failedToUpdatePriorityMessage)
- ✅ "Active mirror: $best" - success message (уже есть activeMirrorText)
- ✅ "Failed to set best mirror: $e" - error message (уже есть failedToSetBestMirrorMessage)
- ✅ "URL скопирован в буфер обмена" - success message (уже есть urlCopiedToClipboardMessage)
- ✅ "Удалить зеркало?" - dialog title (уже есть deleteMirrorTitle)
- ✅ "Внимание: это зеркало по умолчанию..." - warning message (уже есть defaultMirrorWarningMessage)
- ✅ "Вы уверены, что хотите удалить это зеркало?" - confirmation message (уже есть confirmDeleteMirrorMessage)
- ✅ "Удалить" - delete button (уже есть deleteButtonText)
- ✅ "Зеркало удалено: $url" - success message (уже есть mirrorDeletedMessage)
- ✅ "Не удалось удалить зеркало: $e" - error message (уже есть failedToDeleteMirrorMessage)
- ✅ "Неверный формат URL" - error message (добавлено invalidUrlFormatMessage)
- ✅ "Это зеркало уже существует в списке" - error message (уже есть mirrorAlreadyExistsMessage)
- ✅ "Только активные" - filter label (уже есть onlyActiveFilterText)
- ✅ "Только здоровые" - filter label (уже есть onlyHealthyFilterText)
- ✅ "По приоритету" - sort label (уже есть sortByPriorityText)
- ✅ "По здоровью" - sort label (уже есть sortByHealthText)
- ✅ "По скорости" - sort label (уже есть sortBySpeedText)
- ✅ "Нет зеркал, соответствующих фильтрам" - empty message (уже есть noMirrorsMatchFiltersMessage)
- ✅ "Сбросить фильтры" - button text (уже есть resetFiltersButton)
- ✅ "Здоровье: $healthScore%" - health text (уже есть healthScoreText)
- ✅ "Проверено: ${_formatDate(lastOk)}" - checked text (уже есть lastCheckedText)
- ✅ "Установить как активное" - tooltip (уже есть setAsActiveTooltip)
- ✅ "Копировать URL" - tooltip (уже есть copyUrlTooltip)
- ✅ "Удалить зеркало" - tooltip (уже есть deleteMirrorTooltip)
- ✅ "Set Best Mirror" - button text (уже есть setBestMirrorButtonText)

### 7. Player Screen (lib/features/player/presentation/screens/player_screen.dart)

- ✅ "Player: ${widget.bookId}" - appBar title (уже есть playerScreenTitle)
- ✅ "Failed to load audiobook" - error message (уже есть failedToLoadAudioMessage)
- ✅ "Retry" - retry button (уже есть retry)
- ✅ "by ${\_audiobook!.author}" - author text (уже есть byAuthorText)
- ✅ "Chapters" - chapters title (уже есть chaptersLabelText)
- ✅ "Download functionality coming soon!" - download message (уже есть downloadFunctionalityComingSoonMessage)
- ✅ "Sample Audiobook" - sample title (уже есть sampleTitleText)
- ✅ "Sample Author" - sample author (уже есть sampleAuthorText)
- ✅ "Fiction" - sample category (уже есть sampleCategoryText)
- ✅ "150 MB" - sample size (уже есть sampleSizeText)
- ✅ "Chapter 1" - sample chapter (уже есть sampleChapter1Text)
- ✅ "Chapter 2" - sample chapter (уже есть sampleChapter2Text)

### 8. Topic Screen (lib/features/topic/presentation/screens/topic_screen.dart)

- ✅ "Topic: ${widget.topicId}" - appBar title (уже есть topicScreenTitle)
- ✅ "Request timed out. Please check your connection." - timeout message (уже есть requestTimedOutMessage)
- ✅ "Network error: ${e.message}" - network error message (уже есть networkErrorMessage)
- ✅ "Error loading topic: $e" - error message (уже есть errorLoadingTopicMessage)
- ✅ "Failed to load topic" - error message (уже есть failedToLoadTopicMessage)
- ✅ "Retry" - retry button (уже есть retry)
- ✅ "Data loaded from cache" - cache message (уже есть dataLoadedFromCacheMessage)
- ✅ "$seeders seeders" - seeders text (уже есть seedersLabel)
- ✅ "$leechers leechers" - leechers text (уже есть leechersLabel)
- ✅ "Magnet Link" - magnet link title (уже есть magnetLinkLabelText)
- ✅ "Magnet link copied to clipboard" - copy success message (уже есть magnetLinkCopiedMessage)
- ✅ "$label copied to clipboard" - copy success message (уже есть copyToClipboardMessage)
- ✅ "Unknown chapter" - unknown chapter text (уже есть unknownChapterText)
- ✅ "Copy Magnet Link" - menu item (добавлено copyMagnetLink)
- ✅ "Download Torrent" - menu item (добавлено downloadTorrentMenu)
- ✅ "Open torrent file in external app" - subtitle (добавлено openTorrentInExternalApp)
- ✅ "Failed to open torrent: $e" - error message (добавлено failedToOpenTorrent)

### 9. Search Screen (lib/features/search/presentation/screens/search_screen.dart)

- ✅ "Request timed out. Please check your connection." - timeout message (уже есть requestTimedOutMessage)
- ✅ "Network error: ${e.message}" - network error message (уже есть networkErrorMessage)
- ✅ "Error: $e" - error message (нужно добавить общий ключ)
- ✅ "Navigate to login screen" - navigation text (уже есть loginRequiredForSearch)
- ✅ "Подождите, выполняется биометрическая аутентификация..." - status message (уже есть biometricAuthInProgress)
- ✅ "Авторизация успешна" - success message (уже есть authorizationSuccessful)
- ✅ "Авторизация" - dialog title (уже есть authorizationTitle)
- ✅ "Биометрическая аутентификация недоступна или не удалась. Открыть WebView для входа?" - dialog content (уже есть biometricUnavailableMessage)
- ✅ "Отмена" - cancel button (уже есть cancel)
- ✅ "Открыть WebView" - button text (уже есть openWebViewButton)
- ✅ "Биометрическая аутентификация недоступна. Открыть WebView для входа?" - dialog content (уже есть biometricUnavailableMessage)
- ✅ "Ошибка при проверке авторизации: ${e.toString()}" - error message (уже есть authorizationCheckError)
- ✅ "Авторизация не удалась. Проверьте логин и пароль" - error message (уже есть authorizationFailedMessage)
- ✅ "Ошибка открытия страницы авторизации: ${e.toString()}" - error message (уже есть authorizationPageError)
- ✅ "Не удалось подключиться к зеркалам RuTracker. Проверьте подключение к интернету или попробуйте выбрать другое зеркало в настройках" - error message (уже есть mirrorConnectionError)
- ✅ "Не удалось подключиться к зеркалам RuTracker" - error message (уже есть mirrorConnectionFailed)
- ✅ "Фильтры:" - label (добавлено filtersLabel)
- ✅ "Сбросить" - button text (добавлено resetButton)
- ✅ "Другое" - category fallback (добавлено otherCategory)
- ✅ "История поиска" - section title (добавлено searchHistoryTitle)
- ✅ "Очистить" - button text (добавлено clearButton)
- ✅ "Не удалось добавить в избранное" - error message (добавлено failedToAddToFavorites)
- ✅ "Не удалось удалить из избранного" - error message (уже есть failedToRemoveFromFavorites)
- ✅ "Load more" - button text (добавлено loadMoreButton)
- ✅ "Failed to parse search results due to encoding issue..." - error message (добавлено failedToParseSearchResultsEncoding)
- ✅ "Failed to parse search results. The page structure may have changed..." - error message (добавлено failedToParseSearchResultsStructure)
- ✅ "Search failed: $errorType" - error message (добавлено searchFailedMessage)

### 10. Core/Permissions (lib/core/permissions/permission_service.dart, permission_service_v2.dart)

- ⚠️ "Отмена" - cancel button (уже есть cancel, но нужно проверить использование)
- ✅ "Разрешить" - allow button (уже есть allowButton)
- ✅ "Permissions Required" - dialog title (уже есть permissionsRequired)
- ✅ "JaBook needs the following permissions to work properly:" - explanation (уже есть permissionExplanation)
- ✅ "Storage: To save audiobook files and cache data" - storage description (нужно добавить отдельный ключ)
- ✅ "Notifications: To show playback controls and updates" - notification description (нужно добавить отдельный ключ)
- ✅ "Grant Permissions" - button text (уже есть grantPermissions)
- ⚠️ "Cancel" - cancel button (уже есть cancel)

### 11. Core/Auth (lib/core/auth/rutracker_auth.dart)

- ✅ "Login to RuTracker" - dialog title (уже есть authDialogTitle)
- ✅ "Cancel" - cancel button (уже есть cancel)
- ✅ "Logout failed" - error message (уже есть logoutErrorMessage, используется в auth_screen)
- ✅ "Cookie sync failed: ${e.toString()}" - error message (только в логах, не в UI)

### 12. Core/Player (lib/core/player/audio_service_handler.dart)

- ✅ "Failed to start audio service" - error message (добавлено failedToStartAudioService, используется в AudioFailure - core слой без BuildContext)
- ✅ "Failed to play media" - error message (добавлено failedToPlayMedia, используется в AudioFailure - core слой без BuildContext)
- ✅ "Failed to pause media" - error message (добавлено failedToPauseMedia, используется в AudioFailure - core слой без BuildContext)
- ✅ "Failed to stop media" - error message (добавлено failedToStopMedia, используется в AudioFailure - core слой без BuildContext)
- ✅ "Failed to seek" - error message (добавлено failedToSeek, используется в AudioFailure - core слой без BuildContext)
- ✅ "Failed to set speed" - error message (добавлено failedToSetSpeed, уже есть failedToChangeSpeed и используется в local_player_screen)

### 13. Core/Logging (lib/core/logging/structured_logger.dart)

- ✅ "Failed to initialize logger" - error message (добавлено failedToInitializeLogger, используется в LoggingFailure - core слой без BuildContext)
- ✅ "Failed to write log" - error message (добавлено failedToWriteLog, используется в LoggingFailure - core слой без BuildContext)
- ✅ "Log rotation failed" - error message (добавлено logRotationFailed, используется в debugPrint - техническое сообщение)
- ✅ "Failed to rotate logs" - error message (добавлено failedToRotateLogs, используется в LoggingFailure - core слой без BuildContext)
- ✅ "Failed to clean old logs" - error message (добавлено failedToCleanOldLogs, используется в LoggingFailure - core слой без BuildContext)
- ✅ "Failed to export logs" - error message (уже есть failedToExportLogsMessage)
- ✅ "Error sharing logs: $e" - error message (добавлено errorSharingLogs, используется в debugPrint - техническое сообщение)
- ✅ "Failed to share logs" - error message (добавлено failedToShareLogs, используется в LoggingFailure - core слой без BuildContext)
- ✅ "Failed to read logs" - error message (добавлено failedToReadLogs, используется в LoggingFailure - core слой без BuildContext)

### 14. Core/Stream (lib/core/stream/local_stream_server.dart)

- ✅ "Local stream server started on http://$_host:$\_port" - success message (добавлено localStreamServerStarted, используется в print/log - техническое сообщение)
- ✅ "Failed to start stream server: ${e.toString()}" - error message (добавлено failedToStartStreamServer, используется в StreamFailure - core слой без BuildContext)
- ✅ "Local stream server stopped" - success message (добавлено localStreamServerStopped, используется в print/log - техническое сообщение)
- ✅ "Failed to stop stream server: ${e.toString()}" - error message (добавлено failedToStopStreamServer, используется в StreamFailure - core слой без BuildContext)
- ✅ "Missing book ID parameter" - error message (добавлено missingBookIdParameter, используется в HTTP ответах - core слой без BuildContext)
- ✅ "Invalid file index parameter" - error message (добавлено invalidFileIndexParameter, используется в HTTP ответах - core слой без BuildContext)
- ✅ "File not found" - error message (добавлено fileNotFound, используется в HTTP ответах - core слой без BuildContext)
- ✅ "Streaming error" - error message (добавлено streamingError, используется в HTTP ответах - core слой без BuildContext)
- ✅ "Invalid range header" - error message (добавлено invalidRangeHeader, используется в HTTP ответах - core слой без BuildContext)
- ✅ "Requested range not satisfiable" - error message (добавлено requestedRangeNotSatisfiable, используется в HTTP ответах - core слой без BuildContext)
- ✅ "Range request error" - error message (добавлено rangeRequestError, используется в HTTP ответах - core слой без BuildContext)
- ✅ "Static file error" - error message (добавлено staticFileError, используется в HTTP ответах - core слой без BuildContext)

### 15. Core/Cache (lib/core/cache/cache_manager.dart)

- ✅ "CacheManager not initialized" - error message (добавлено cacheManagerNotInitialized, используется в StateError - core слой без BuildContext)

### 16. Core/Parse (lib/core/parse/rutracker_parser.dart, category_parser.dart)

- ✅ "Failed to parse search results" - error message (добавлено failedToParseSearchResults, используется в ParsingFailure - core слой без BuildContext)
- ✅ "Failed to parse topic details" - error message (добавлено failedToParseTopicDetails, используется в ParsingFailure - core слой без BuildContext)
- ✅ "Failed to parse categories" - error message (добавлено failedToParseCategories, используется в ParsingFailure - core слой без BuildContext)
- ✅ "Failed to parse category topics" - error message (добавлено failedToParseCategoryTopics, используется в ParsingFailure - core слой без BuildContext)
- ✅ "Радиоспектакль" - category (добавлено radioPlayCategory, используется в парсинге - core слой без BuildContext, но отображается в UI)
- ✅ "Аудиокнига" - category (добавлено audiobookCategory, используется в парсинге - core слой без BuildContext, но отображается в UI)
- ✅ "Биография" - category (добавлено biographyCategory, используется в парсинге - core слой без BuildContext, но отображается в UI)
- ✅ "Мемуары" - category (добавлено memoirsCategory, используется в парсинге - core слой без BuildContext, но отображается в UI)
- ✅ "История" - category (добавлено historyCategory, используется в парсинге - core слой без BuildContext, но отображается в UI)
- ✅ "Другое" - category (уже есть otherCategory)
- ✅ "Добавлено" - date label (добавлено addedLabel, используется в парсинге даты - техническая строка)
- ✅ "Сиды" - seeders label (уже есть seedersLabel)
- ✅ "Личи" - leechers label (уже есть leechersLabel)

### 17. Core/Torrent (lib/core/torrent/audiobook_torrent_manager.dart)

- ✅ "Invalid magnet URL: missing info hash" - error message (добавлено invalidMagnetUrlMissingHash, используется в TorrentFailure - core слой без BuildContext)
- ✅ "Invalid info hash length" - error message (добавлено invalidInfoHashLength, используется в TorrentFailure - core слой без BuildContext)
- ✅ "Failed to start download: ${e.toString()}" - error message (добавлено failedToStartDownloadWithError, уже есть failedToStartDownload без параметра, используется в TorrentFailure - core слой без BuildContext)
- ✅ "Download not found" - error message (добавлено downloadNotFoundTorrent, уже есть downloadNotFound, используется в TorrentFailure - core слой без BuildContext)
- ✅ "Failed to pause download: ${e.toString()}" - error message (добавлено failedToPauseDownload, используется в TorrentFailure - core слой без BuildContext)
- ✅ "Failed to resume download: ${e.toString()}" - error message (добавлено failedToResumeDownload, используется в TorrentFailure - core слой без BuildContext)
- ✅ "Failed to remove download: ${e.toString()}" - error message (добавлено failedToRemoveDownload, используется в TorrentFailure - core слой без BuildContext)
- ✅ "Failed to get active downloads: ${e.toString()}" - error message (добавлено failedToGetActiveDownloads, используется в TorrentFailure - core слой без BuildContext)
- ✅ "Failed to shutdown torrent manager: ${e.toString()}" - error message (добавлено failedToShutdownTorrentManager, используется в TorrentFailure - core слой без BuildContext)

### 18. Core/Endpoints (lib/core/endpoints/endpoint_manager.dart)

- ✅ "No healthy endpoints available" - error message (добавлено noHealthyEndpointsAvailable, используется в логах - техническое сообщение)

### 19. Core/Config (lib/core/config/app_config.dart)

- ✅ "AuthRepositoryProvider must be overridden with proper context" - error message (добавлено authRepositoryProviderMustBeOverridden, используется в @Deprecated - техническое сообщение)
- ✅ "Use EndpointManager.getActiveEndpoint() for dynamic mirror selection" - deprecated message (добавлено useEndpointManagerGetActiveEndpoint, используется в @Deprecated - техническое сообщение)
- ✅ "CacheManager not initialized" - error message (добавлено cacheManagerNotInitializedConfig, уже есть cacheManagerNotInitialized)

### 20. Core/Net (lib/core/net/dio_client.dart)

- ✅ "Search failed: ${e.message}" - error message (добавлено searchFailedWithMessage, уже есть searchFailedMessage)
- ✅ "Failed to search audiobooks" - error message (добавлено failedToSearchAudiobooks, используется в NetworkFailure - core слой без BuildContext)
- ✅ "Failed to fetch categories: ${e.message}" - error message (добавлено failedToFetchCategories, используется в NetworkFailure - core слой без BuildContext)
- ✅ "Failed to get categories" - error message (добавлено failedToGetCategories, используется в NetworkFailure - core слой без BuildContext)
- ✅ "Failed to get category audiobooks: ${e.message}" - error message (добавлено failedToGetCategoryAudiobooksWithMessage, используется в NetworkFailure - core слой без BuildContext)
- ✅ "Failed to get category audiobooks" - error message (добавлено failedToGetCategoryAudiobooks, используется в NetworkFailure - core слой без BuildContext)
- ✅ "Failed to fetch audiobook details: ${e.message}" - error message (добавлено failedToFetchAudiobookDetails, используется в NetworkFailure - core слой без BuildContext)
- ✅ "Failed to get audiobook details" - error message (добавлено failedToGetAudiobookDetails, используется в NetworkFailure - core слой без BuildContext)
- ✅ "Failed to fetch new releases: ${e.message}" - error message (добавлено failedToFetchNewReleases, используется в NetworkFailure - core слой без BuildContext)

### 21. Core/Auth/Credential Manager (lib/core/auth/credential_manager.dart)

- ✅ "Failed to save credentials: ${e.toString()}" - error message (добавлено failedToSaveCredentials, используется в AuthFailure - core слой без BuildContext)
- ✅ "Failed to retrieve credentials: ${e.toString()}" - error message (добавлено failedToRetrieveCredentials, используется в AuthFailure - core слой без BuildContext)
- ✅ "Failed to clear credentials: ${e.toString()}" - error message (добавлено failedToClearCredentials, используется в AuthFailure - core слой без BuildContext)
- ✅ "No credentials to export" - error message (добавлено noCredentialsToExport, используется в AuthFailure - core слой без BuildContext)
- ✅ "Unsupported export format: $format" - error message (добавлено unsupportedExportFormat, используется в AuthFailure - core слой без BuildContext)
- ✅ "Invalid CSV format" - error message (добавлено invalidCsvFormat, используется в FormatException - core слой без BuildContext)
- ✅ "Invalid CSV data" - error message (добавлено invalidCsvData, используется в FormatException - core слой без BuildContext)
- ✅ "Invalid JSON format" - error message (добавлено invalidJsonFormat, используется в FormatException - core слой без BuildContext)
- ✅ "Unsupported import format: $format" - error message (добавлено unsupportedImportFormat, используется в AuthFailure - core слой без BuildContext)
- ✅ "Failed to import credentials: ${e.toString()}" - error message (добавлено failedToImportCredentials, используется в AuthFailure - core слой без BuildContext)

### 22. Core/Net/Cloudflare Utils (lib/core/net/cloudflare_utils.dart)

- ✅ "checking your browser" - cloudflare check (техническая строка для обнаружения Cloudflare, не отображается пользователю)
- ✅ "please enable javascript" - cloudflare check (техническая строка для обнаружения Cloudflare, не отображается пользователю)
- ✅ "attention required" - cloudflare check (техническая строка для обнаружения Cloudflare, не отображается пользователю)
- ✅ "cf-chl-bypass" - cloudflare check (техническая строка для обнаружения Cloudflare, не отображается пользователю)
- ✅ "challenges.cloudflare.com" - cloudflare check (техническая строка для обнаружения Cloudflare, не отображается пользователю)
- ✅ "cf-turnstile" - cloudflare check (техническая строка для обнаружения Cloudflare, не отображается пользователю)

### 23. Core/Parse/Category Parser (lib/core/parse/category_parser.dart)

- ✅ "Failed to parse categories" - error message (уже есть failedToParseCategories)
- ✅ "Failed to parse category topics" - error message (уже есть failedToParseCategoryTopics)

### 24. Core/Net/User Agent Manager (lib/core/net/user_agent_manager.dart)

- ✅ "Failed to generate dynamic user agent: $e" - log message (не нужно локализовать - это технический лог)
- ✅ "Failed to get user agent: $e" - log message (не нужно локализовать - это технический лог)
- ✅ "Failed to store user agent: $e" - log message (не нужно локализовать - это технический лог)
- ✅ "Failed to clear user agent: $e" - log message (не нужно локализовать - это технический лог)

### 25. Core/Parse/Rutracker Parser (lib/core/parse/rutracker_parser.dart)

- ✅ "Failed to parse search results" - error message (уже есть failedToParseSearchResults)
- ✅ "Failed to parse topic details" - error message (уже есть failedToParseTopicDetails)

### 26. App/Router (lib/app/router/app_router.dart)

- ✅ "Error" - error title (добавлено error, используется)
- ✅ "No audiobook group provided" - error message (добавлено noAudiobookGroupProvided)
- ⚠️ "Downloads" - navigation title (уже есть downloadsTitle, но используется хардкод в строке 182 как fallback - это нормально)
- ⚠️ "Settings" - navigation title (уже есть navSettings, но используется хардкод в строке 187 как fallback - это нормально)
- ⚠️ "Debug" - navigation title (уже есть navDebug, но используется хардкод в строке 200 как fallback - это нормально)
- ✅ "Library" - navigation title (уже есть navLibrary)
- ✅ "Connect" - navigation title (уже есть navAuth)
- ✅ "Search" - navigation title (уже есть navSearch)

### 27. App/App (lib/app/app.dart)

- ⚠️ "App initialization complete" - log message (не нужно локализовать - это логи)
- ⚠️ "Initializing JaBook app..." - log message (не нужно локализовать)
- ⚠️ "Build flavor: ${config.flavor}" - log message (не нужно локализовать)
- ⚠️ "App version: ${config.appVersion}" - log message (не нужно локализовать)
- ⚠️ "API base URL: ${config.apiBaseUrl}" - log message (не нужно локализовать)
- ⚠️ "Log level: ${config.logLevel}" - log message (не нужно локализовать)
- ⚠️ "Unknown flavor: ${config.flavor}, falling back to dev" - log message (не нужно локализовать)
- ⚠️ "Initializing database..." - log message (не нужно локализовать)
- ⚠️ "Database, cache, and endpoints initialized successfully" - log message (не нужно локализовать)
- ⚠️ "Initializing development environment" - log message (не нужно локализовать)
- ⚠️ "Debug features enabled" - log message (не нужно локализовать)
- ⚠️ "Initializing stage environment" - log message (не нужно локализовать)
- ⚠️ "Analytics enabled for stage environment" - log message (не нужно локализовать)
- ⚠️ "Crash reporting enabled for stage environment" - log message (не нужно локализовать)
- ⚠️ "Initializing production environment" - log message (не нужно локализовать)
- ⚠️ "Analytics enabled for production environment" - log message (не нужно локализовать)
- ⚠️ "Crash reporting enabled for production environment" - log message (не нужно локализовать)
- ⚠️ "Debug features disabled for production environment" - log message (не нужно локализовать)

### 28. Favorites Screen (lib/features/library/presentation/screens/favorites_screen.dart)

- ✅ "Избранное" - appBar title (уже есть favoritesTitle)
- ✅ "Обновить" - refresh tooltip (уже есть refreshTooltip)
- ✅ "Нет избранных аудиокниг" - empty state message (уже есть noFavoritesMessage)
- ✅ "Добавьте аудиокниги в избранное из результатов поиска" - empty state hint (уже есть addFavoritesHint)
- ✅ "Перейти к поиску" - button text (уже есть goToSearchButton)
- ✅ "Удалено из избранного" - success message (уже есть removedFromFavorites)
- ✅ "Не удалось удалить из избранного" - error message (уже есть failedToRemoveFromFavorites)

### 29. Permissions Onboarding Dialog (lib/features/permissions/presentation/widgets/permissions_onboarding_dialog.dart)

- ✅ "Разрешения для JaBook" - dialog title (добавлено permissionsForJaBookTitle)
- ✅ "Доступ к файлам" - permission title (добавлено fileAccessPermissionTitle)
- ✅ "Нужен для сохранения и воспроизведения аудиокниг." - permission description (добавлено fileAccessPermissionDescription)
- ✅ "Уведомления" - permission title (добавлено notificationsPermissionTitle)
- ✅ "Для управления воспроизведением из уведомлений." - permission description (используется существующий notificationsPermissionDescription)
- ✅ "Оптимизация батареи" - permission title (добавлено batteryOptimizationPermissionTitle)
- ✅ "Чтобы приложение работало в фоне для воспроизведения." - permission description (добавлено batteryOptimizationPermissionDescription)
- ✅ "Эти разрешения помогут обеспечить лучший опыт использования приложения." - explanation (добавлено permissionsHelpMessage)
- ✅ "Продолжить" - continue button (добавлено continueButton)

### 30. Background Compatibility Banner (lib/features/permissions/presentation/widgets/background_compatibility_banner.dart)

- ✅ "Background Operation Settings" - banner title (уже есть backgroundCompatibilityBannerTitle, используется как fallback)
- ✅ "To ensure stable background operation, you may need to configure device settings." - banner message (уже есть backgroundCompatibilityBannerMessage, используется как fallback)
- ✅ "Dismiss" - tooltip (уже есть dismiss, используется как fallback)

### 31. System Capabilities Widget (lib/features/settings/presentation/widgets/system_capabilities_widget.dart)

- ✅ "Ошибка проверки возможностей: $e" - error message (добавлено capabilityCheckError)
- ✅ "Выбрано файлов: ${files.length}" - success message (добавлено filesSelected)
- ✅ "Ошибка выбора файлов: $e" - error message (добавлено fileSelectionError)
- ✅ "Выбрано изображений: ${images.length}" - success message (добавлено imagesSelected)
- ✅ "Ошибка выбора изображений: $e" - error message (добавлено imageSelectionError)
- ✅ "Тест уведомления" - notification title (добавлено testNotificationTitle)
- ✅ "Это тестовое уведомление от JaBook" - notification body (добавлено testNotificationBody)
- ✅ "Уведомление отправлено" - success message (добавлено notificationSent)
- ✅ "Не удалось отправить уведомление (канал не реализован)" - error message (добавлено failedToSendNotification)
- ✅ "Сопряженных устройств: ${pairedDevices.length}" - status message (добавлено pairedDevicesCount)
- ✅ "Ошибка проверки Bluetooth: $e" - error message (добавлено bluetoothCheckError)
- ✅ "Системные возможности" - section title (добавлено systemCapabilitiesTitle)
- ✅ "Доступ к файлам" - capability name (добавлено fileAccessCapability)
- ✅ "Доступ к изображениям" - capability name (добавлено imageAccessCapability)
- ✅ "Камера" - capability name (добавлено cameraCapability)
- ✅ "Фото сделано: $photo" - success message (добавлено photoTaken)
- ✅ "Фото не сделано" - error message (добавлено photoNotTaken)
- ✅ "Ошибка камеры: $e" - error message (добавлено cameraError)
- ✅ "Уведомления" - capability name (добавлено notificationsCapability)
- ✅ "Объяснение возможностей" - button text (добавлено capabilityExplanationButton)
- ✅ "Тест" - test button (добавлено testButton)

### 32. Mirrors Screen (lib/features/mirrors/presentation/screens/mirrors_screen.dart)

- ✅ "Активное зеркало установлено: $url" - success message (уже есть activeMirrorSetMessage, заменено в коде)
- ✅ "Не удалось установить активное зеркало: $e" - error message (уже есть failedToSetActiveMirrorMessage, заменено в коде)

### 33. Library Repository (lib/features/library/data/repositories/rutracker_repository_impl.dart)

- ✅ "Посл. сообщение" - sort option (уже есть lastMessageSort, добавлено topicTitleSort и postTimeSort в ARB, но используется в data layer без BuildContext - это fallback значения)
- ✅ "Название темы" - sort option (добавлено topicTitleSort)
- ✅ "Время размещения" - sort option (добавлено postTimeSort)

### 34. Library Screen - Filter Dialog (lib/features/library/presentation/screens/library_screen.dart)

- ✅ "Reset" - button text (добавлено resetButton)
- ✅ "Apply" - button text (добавлено applyButton)
- ✅ "File picker is already open. Please close it first." - error message (добавлено filePickerAlreadyOpen)

### 35. Trash Screen (lib/features/library/presentation/screens/trash_screen.dart)

- ✅ "Failed to load trash: $e" - error message (добавлено failedToLoadTrash)
- ✅ "Item restored successfully" - success message (добавлено itemRestoredSuccessfully)
- ✅ "Failed to restore item" - error message (добавлено failedToRestoreItem)
- ✅ "Error restoring item: $e" - error message (добавлено errorRestoringItem)
- ✅ "Permanently Delete" - dialog title (добавлено permanentlyDeleteTitle)
- ✅ "Are you sure you want to permanently delete \"${item.groupName}\"? This action cannot be undone." - confirmation message (добавлено permanentlyDeleteMessage)
- ✅ "Cancel" - cancel button (уже есть cancel)
- ✅ "Delete" - delete button (добавлено deleteButtonText)
- ✅ "Item permanently deleted" - success message (добавлено itemPermanentlyDeleted)
- ✅ "Failed to delete item" - error message (добавлено failedToDeleteItem)
- ✅ "Error deleting item: $e" - error message (добавлено errorDeletingItem)
- ✅ "Clear All Trash" - dialog title (добавлено clearAllTrashTitle)
- ✅ "Are you sure you want to permanently delete all items in trash? This action cannot be undone." - confirmation message (добавлено clearAllTrashMessage)

### 36. Local Player Screen (lib/features/player/presentation/screens/local_player_screen.dart)

- ✅ "Sleep timer: Playback paused" - notification message (добавлено sleepTimerPaused)
- ✅ "Failed to change speed: ${e.message}" - error message (добавлено failedToChangeSpeed)
- ✅ "Retry" - retry button (уже есть retry)
- ✅ "Cancel timer" - button text (добавлено cancelTimerButton)
- ✅ "End of chapter" - label text (добавлено endOfChapterLabel)
- ✅ "At end of chapter" - label text (добавлено atEndOfChapterLabel)

### 37. Settings Screen - Additional Strings (lib/features/settings/presentation/screens/settings_screen.dart)

- ✅ "Allow" - button text (добавлено allowButton, используется)
- ✅ "Open Settings" - button text (добавлено openSettingsButton)
- ✅ "Совместимость и диагностика" - section title (добавлено compatibilityDiagnosticsTitle)
- ✅ "Проверка совместимости и настройка параметров производителя" - section subtitle (добавлено compatibilityDiagnosticsSubtitle)
- ✅ "Select Folder" - button text (добавлено selectFolderButton)
- ✅ "Continue" - button text (добавлено continueButton)
- ✅ "Error adding folder: ${e.toString()}" - error message (добавлено errorAddingFolder)
- ✅ "Configure and test RuTracker mirrors" - subtitle (добавлено configureMirrorsSubtitle, используется)

### 38. App Initialization (lib/app/app.dart)

- ✅ "Initialization error: ${e.toString()}" - error message (добавлено initializationError)
- ✅ "Loading..." - loading message (уже есть loading, используется хардкод в строке 991 как fallback до инициализации локализации - это нормально)
- ✅ "Error: ${e.toString()}" - error message (добавлено errorWithDetails, заменено в коде)
- ✅ "Critical error: ${e.toString()}" - error message (добавлено criticalError)

### 39. Storage Management Screen (lib/features/library/presentation/screens/storage_management_screen.dart)

- ✅ "Items in Trash" - label text (добавлено itemsInTrashLabel)
- ✅ "Trash Size" - label text (добавлено trashSizeLabel)
- ✅ "Manage Trash" - button text (добавлено manageTrashButton)
- ✅ "Audiobook Groups" - section title (уже есть audiobookGroupsTitle, используется хардкод как fallback - это нормально)
- ✅ "Deselect All" - button text (уже есть deselectAllButton, используется хардкод как fallback - это нормально)
- ✅ "Select All" - button text (уже есть selectAllButton, используется хардкод как fallback - это нормально)
- ✅ "No audiobooks found" - empty message (уже есть noAudiobooksMessage, используется хардкод как fallback - это нормально)
- ✅ "file" / "files" - file count text (добавлено fileSingular и filePlural, заменено в коде)
- ✅ "Delete" - button text (уже есть deleteFilesButton, используется хардкод как fallback - это нормально)
- ✅ "Deleting..." - status message (уже есть deletingMessage, используется хардкод как fallback - это нормально)
- ✅ "Files deleted successfully" - success message (уже есть deletedSuccessMessage, используется хардкод как fallback - это нормально)
- ✅ "Failed to delete files" - error message (уже есть deleteFailedMessage, используется хардкод как fallback - это нормально)
- ✅ "Removed from library successfully" - success message (уже есть removedFromLibrarySuccessMessage, используется хардкод как fallback - это нормально)
- ✅ "Cannot delete: File is currently being played" - error message (уже есть fileInUseMessage, используется хардкод как fallback - это нормально)
- ✅ "Failed to delete: ${e.toString()}" - error message (уже есть deleteFailedMessage, используется хардкод как fallback - это нормально)
- ✅ "Delete Selected?" - dialog title (уже есть deleteSelectedTitle, используется хардкод как fallback - это нормально)
- ✅ "Are you sure you want to delete ${_selectedGroups.length} selected audiobook(s)?" - confirmation message (добавлено deleteSelectedAudiobooksConfirmation, заменено в коде)
- ✅ "Cancel" - cancel button (уже есть cancel, используется хардкод как fallback - это нормально)
- ✅ "Deleted: $successCount, Failed: $failCount" - result message (уже есть deleteSelectedResultMessage, используется хардкод как fallback - это нормально)
- ✅ "Clear Playback Cache?" - dialog title (уже есть clearPlaybackCacheTitle, используется хардкод как fallback - это нормально)
- ✅ "This will clear the playback cache. Playback may be slower until cache is rebuilt." - confirmation message (добавлено clearPlaybackCacheDescription, заменено в коде)
- ✅ "Clear" - button text (уже есть clearButton, используется хардкод как fallback - это нормально)
- ✅ "Clearing cache..." - status message (уже есть clearingCacheMessage, используется хардкод как fallback - это нормально)
- ✅ "Cache cleared: ${_formatSize(clearedSize)}" - success message (уже есть cacheClearedSuccessMessage, используется хардкод как fallback - это нормально)
- ✅ "Clear All Cache?" - dialog title (уже есть clearAllCacheTitle, используется хардкод как fallback - это нормально)
- ✅ "This will clear all cache including playback cache, temporary files, and old logs." - confirmation message (добавлено clearAllCacheDescription, заменено в коде)
- ✅ "Clear All" - button text (уже есть clearAllCacheButton, используется хардкод как fallback - это нормально)
- ✅ "All cache cleared successfully" - success message (уже есть cacheClearedSuccessMessage, используется хардкод как fallback - это нормально)

### 40. Downloads Screen (lib/features/downloads/presentation/screens/downloads_screen.dart)

- ✅ "Operation timed out. Please try again." - error message (уже есть requestTimedOutMessage, используется хардкод как fallback - это нормально)
- ✅ "Network error. Please check your connection." - error message (уже есть networkErrorMessage, используется хардкод как fallback - это нормально)
- ✅ "Permission denied. Please check app permissions in settings." - error message (добавлено permissionDeniedDownloads)
- ✅ "Download not found. It may have been removed." - error message (добавлено downloadNotFound)
- ✅ "An error occurred. Please try again." - error message (добавлено anErrorOccurred)

## Статистика локализации

- **Всего секций проверено:** 40
- **Уже локализовано (✅):** ~200+ строк
- **Требует локализации (⚠️):** ~150+ строк
- **Технические логи (не требуют локализации):** ~50+ строк

## Примечания

- ✅ = Уже локализовано
- ⚠️ = Требует локализации
- Логи и технические сообщения не требуют локализации
- Некоторые строки могут быть техническими и не требуют локализации
- Fallback строки (используемые как запасной вариант) помечены как "используется хардкод как fallback" - это нормально, но желательно заменить на локализацию
